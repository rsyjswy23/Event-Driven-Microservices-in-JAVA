spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/hui/Documents/GitHub/microservices-project/config-server-repository
          clone-on-start: true
    fail-fast: true