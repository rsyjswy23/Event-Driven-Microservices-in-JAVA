
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rsyjswy23/microservices-app-config-repository.git
          default-label: master
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}